# Hamed AI — dependencies
#
# The Core (app/agents, app/db, app/tools, app/permissions) needs
# NOTHING beyond Python's standard library — it will import and run
# tests with just `python3`. Everything below is for the OPTIONAL
# adapters. Comment out what you don't need for a given deployment.

# --- Dashboard / Health / Webhook API (app/api/server.py) ---
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0

# --- Telegram channel (app/channels/telegram_adapter.py) ---
python-telegram-bot>=20,<21

# --- AI provider SDKs (wire into app/ai_providers, only if used) ---
openai>=1.30,<2.0
anthropic>=0.30,<1.0

# --- Web search for real Opportunity Hunter / Fact Check data ---
requests>=2.31,<3.0

# --- Optional: Twilio (voice/SMS) and Paymob (payments) ---
# twilio>=9.0,<10.0
