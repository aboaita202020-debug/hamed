# Hamed AI — runtime and test dependencies

# --- Dashboard / Health / Webhook API ---
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0

# --- Telegram runtime ---
pyTelegramBotAPI>=4.16,<6.0

# --- Environment configuration ---
python-dotenv>=1.0,<2.0

# --- AI provider SDKs ---
openai>=1.30,<2.0
anthropic>=0.30,<1.0

# --- Web search / external data ---
requests>=2.31,<3.0

# --- Tests ---
pytest>=8,<9

# --- Optional: Twilio (voice/SMS) and Paymob (payments) ---
# twilio>=9.0,<10.0
