# Google OAuth — put real values in deployment secrets, never GitHub.
GOOGLE_ADMIN_EMAIL=aboaita202020@gmail.com
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=https://YOUR-HAMED-DOMAIN.example.com/oauth/google/callback
# Comma-separated OAuth scopes; start narrow and add scopes only when needed.
GOOGLE_OAUTH_SCOPES=openid,email,https://www.googleapis.com/auth/gmail.readonly
